"""
scripts/audit — Módulo de auditoria do pipeline de comissões.

Submodules:
    invariant_checker  — Verifica invariantes de negócio nos resultados
    trace_collector    — Coleta trilhas de execução por item (opt-in)
    pipeline_diff      — Compara divergências entre faturamento e recebimento
    audit_report       — Gera relatório consolidado de auditoria
"""
